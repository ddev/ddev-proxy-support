#ddev-generated

name: proxy-support

pre_install_actions:

project_files:
- docker-compose.proxy-support.yaml
- web-build/pre.Dockerfile.proxy-support

global_files:

post_install_actions:

yaml_read_files:
